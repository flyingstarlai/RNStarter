import * as types from './actionTypes';

export const navigateToRoot = () => ({
  type: types.NAVIGATION_ROOT,
});

export const navigateToMain = () => ({
  type: types.NAVIGATION_MAIN,
});

export const navigateToAuth = () => ({
  type: types.NAVIGATION_AUTH,
});
